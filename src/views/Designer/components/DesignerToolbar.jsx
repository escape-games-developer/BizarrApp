import {useDesigner} from "../store/designerStore";
import ScreenImporter from "./ScreenImporter";
export default function DesignerToolbar(){const d=useDesigner();return <div className="designer-toolbar"><div className="designer-tools"><button className={d.tool==="select"?"active":""} onClick={()=>d.setTool("select")}>↖ Seleccionar</button><button className={d.tool==="hand"?"active":""} onClick={()=>d.setTool("hand")}>✋ Mano</button><button className={d.guidesEnabled?"active":""} onClick={()=>d.setGuidesEnabled(value=>!value)}>Guías {d.guidesEnabled?"ON":"OFF"}</button><button className={d.snapEnabled?"active":""} onClick={()=>d.setSnapEnabled(value=>!value)}>Snap {d.snapEnabled?"ON":"OFF"}</button></div><details className="advanced-import"><summary>Importar legado</summary><ScreenImporter/></details>{d.document.layoutMode==="responsive"?<div className="viewport-presets">{[[390,844],[430,932],[768,1024]].map(([width,height])=><button key={width} className={d.previewWidth===width?"active":""} onClick={()=>d.setPreviewWidth(width)}>{width}×{height}</button>)}</div>:<span>{d.document.viewport.width} × {d.document.viewport.height}</span>}</div>}
