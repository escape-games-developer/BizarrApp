import { useDesigner } from "../store/designerStore";
export default function DesignerFooter(){const d=useDesigner();return <footer className="designer-footer"><div className="panel-title">ESTADOS / PÁGINAS</div><div className="page-strip">{d.document.pages.map((p,i)=><button key={p.id} className={d.selectedPageId===p.id?"active":""} onClick={()=>{d.setSelectedPageId(p.id);d.setSelectedElementId(null)}}><span className="page-thumb" style={{background:p.background.value}}>{i+1}</span><b>{p.name}</b></button>)}<button className="add-page" onClick={d.addPage}>＋ Nueva pantalla</button></div></footer>}
