const DB="bizarren-designer-assets",STORE="assets";
const openDb=()=>new Promise((resolve,reject)=>{const request=indexedDB.open(DB,1);request.onupgradeneeded=()=>request.result.createObjectStore(STORE,{keyPath:"id"});request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)});
export const readImageMetadata=file=>new Promise(resolve=>{if(!file?.type?.startsWith("image/")){resolve({});return}const url=URL.createObjectURL(file),image=new Image();image.onload=()=>{const metadata={naturalWidth:image.naturalWidth,naturalHeight:image.naturalHeight,aspectRatio:image.naturalWidth/image.naturalHeight};URL.revokeObjectURL(url);resolve(metadata)};image.onerror=()=>{URL.revokeObjectURL(url);resolve({})};image.src=url});
export async function saveDesignerAsset(file){const db=await openDb(),id=`asset-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}`,metadata=await readImageMetadata(file);await new Promise((resolve,reject)=>{const tx=db.transaction(STORE,"readwrite");tx.objectStore(STORE).put({id,name:file.name,type:file.type,blob:file,...metadata});tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)});db.close();return id}
export async function loadDesignerAsset(id){if(!id)return null;const db=await openDb(),record=await new Promise((resolve,reject)=>{const request=db.transaction(STORE).objectStore(STORE).get(id);request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)});db.close();return record||null}
