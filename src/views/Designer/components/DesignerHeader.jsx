import { useDesigner } from "../store/designerStore";
const TARGETS = [["client","Cliente"],["admin","Administrador"],["giant","Pantalla gigante"]];
export default function DesignerHeader(){ const d=useDesigner(); return <header className="designer-header"><div className="designer-title">DISEÑADOR <span>V0.1</span></div><div className="designer-targets">{TARGETS.map(([id,label])=><button key={id} className={d.target===id?"active":""} onClick={()=>d.setTarget(id)}>{label}</button>)}</div><div className="designer-actions"><span>{d.savedAt?`Guardado ${d.savedAt.toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}`:"Borrador local"}</span><button onClick={d.undo} title="Ctrl+Z">↶</button><button onClick={d.redo} title="Ctrl+Shift+Z">↷</button><button onClick={()=>d.setPreview(true)}>Vista previa</button><button onClick={d.save}>Guardar borrador</button><button className="primary" onClick={d.publish}>Publicar</button></div></header>; }
